# プル(updateさせる）、プッシュ(updateする)

# # clone: git clone UIR

#statusの概念(saveの感じ)
    #. 3
    # git add. (現在の（全部）ファイルをstatusされる（commitの対象を追加する感じ))
    # git status で確認できる
#commit (add した後で出来る)
    # git commit - m '話したいメッセージ'



def main ():
    print("hello git_hub!!")